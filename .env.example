# Copy this file to .env.local and fill in your key.
# Without a key, the app runs in DEMO MODE using a built-in sample policy.

# OpenAI API key used server-side for the analysis (never exposed to the client).
OPENAI_API_KEY=
